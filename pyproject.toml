[project]
name = "ABAH_CHAT"
version = "1.0.0"
dependencies = [
  "autogen-agentchat==0.7.5",
  "autogen-core==0.7.5",
  "autogen-ext==0.7.5",
  "ollama==0.6.2",
  "tiktoken"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
abah_chat = "abah_chat.main:ABAH_CHAT"
